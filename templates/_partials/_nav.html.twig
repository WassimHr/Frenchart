<nav>
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<div class="container-fluid d-flex justify-content-between">
			<div>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon navbar-dark"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<h4>
								<a class="nav-link active" aria-current="page" href="{{path('main')}}">French'Art</a>
							</h4>
						</li>
						<li class="nav-item">
							
									<a class="nav-link hover-animation-peinture paint" href="#">Peinture</a>

						</li>
						<li class="nav-item">
							<a class="nav-link hover-animation-sculture" href="#">Sculture</a>
						</li>
						<li class="nav-item">
							<a class="nav-link hover-animation-photo" href="#">Photographie</a>
						</li>
					</ul>
				</div>
			</div>
			<div>

				<ul class="navbar-nav">
					{% if app.user %}
						<li class="nav-item">
							<a style="grey" class="nav-link log" href="{{ path('app_logout') }}">Deconnexion</a>
						</li>
					{% else %}
						<li class="nav-item">
							<a style="grey" class="nav-link log" href="{{ path('app_register') }}">Inscription</a>
						</li>
						<li class="nav-item">
							<a style="margin-top:-2%; color:grey;" class="nav-link log" href="{{ path('app_login') }}">Connexion</a>
						</li>
					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
</nav>

{# <nav>
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
		<div class="container-fluid d-flex justify-content-between">
			<div>
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon navbar-dark"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<h4>
								<a class="nav-link active" aria-current="page" href="{{ path('main')}}">French'Art</a>
							</h4>
						</li>
						{% for category in categories %}
							<li class="nav-item">
								{% if category.name == 'Peinture' %}
									<a class="nav-link hover-animation-peinture paint" href="{{ path('categories_list', {slug: category.slug})}}">{{ category.name }}</a>
								{% elseif category.name == 'Sculture' %}
									<a class="nav-link hover-animation-sculture" href="{{ path('categories_list', {slug: category.slug})}}">{{ category.name }}</a>
								{% elseif category.name == 'Photographie' %}
									<a class="nav-link hover-animation-photo" href="{{ path('categories_list', {slug: category.slug})}}">{{ category.name }}</a>
								{% endif %}
							</li>
						{% endfor %}
					</ul>
				</div>
			</div>
			<div>

				<ul class="navbar-nav">
					{% if app.user %}
						<li class="nav-item">
							<a style="grey" class="nav-link log" href="{{ path('app_logout') }}">Deconnexion</a>
						</li>
					{% else %}
						<li class="nav-item">
							<a style="grey" class="nav-link log" href="{{ path('app_register') }}">Inscription</a>
						</li>
						<li class="nav-item">
							<a style="margin-top:-2%; color:grey;" class="nav-link log" href="{{ path('app_login') }}">Connexion</a>
						</li>
					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
</nav>   #}
